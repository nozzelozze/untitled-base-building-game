using System.Collections.Generic;
using SFML.Graphics;
using SFML.System;

/* public class StructureInfoMenu : GUIElement
{

    GUIText TitleText;
    GUIText InfoText;

    Structure ElementStructure;

    public StructureInfoMenu(Structure structure)
    : base(new GUIElementConfig{ StartPosition = new Vector2f(0,0), Style = StyleManager.DefaultStyle })
    {
        ElementStructure = structure;
        TitleText = new GUIText(
            structure.Name,
            new GUIElementConfig{ StartPosition = new Vector2f(), RelativeTo = Position }  
        );
        //string info = "Storage: 50%nHejsan: 2p";
        //InfoText = new GUIText(
        //    info,
        //    new GUIElementConfig{ }
        //)
    }

    public override void Update()
    {
        base.Update();

    }

} */